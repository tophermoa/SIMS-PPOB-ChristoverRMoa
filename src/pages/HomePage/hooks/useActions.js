import { useEffect } from 'react';

const useActions = () => {
  // TODO: Fetch profile, balance, services, and banners on mount
  useEffect(() => {
    // dispatch(fetchProfile());
    // dispatch(fetchBalance());
    // dispatch(fetchServices());
    // dispatch(fetchBanners());
  }, []);

  return {};
};

export default useActions;
